services:
  postgres:
    image: postgres:16
    container_name: clinic-appointment-db
    environment:
      POSTGRES_DB: clinic_appointment
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: